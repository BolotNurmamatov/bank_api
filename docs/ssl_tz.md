Тема: Привязка домена и настройка HTTPS для дашборда казначейства (API Bank)

Привет! Нам нужно перевести наш новый дашборд с IP-адреса на красивый корпоративный домен (например, treasury.redpetroleum.kg или bank.redpetroleum.kg).

Что нужно сделать:

Настроить DNS: Создать A-запись для желаемого поддомена, которая будет вести на IP-адрес нашего сервера: [УКАЖИТЕ IP ВАШЕГО СЕРВЕРА].
Настроить Nginx (Reverse Proxy):
Принимать трафик по новому домену на 80/443 портах.
Настроить проксирование запросов к Frontend-части (Next.js), которая крутится на localhost:3000 (или на том порту, где запущен frontend через PM2).
Настроить проксирование запросов /api/ к Backend-части (FastAPI), которая запущена на localhost:8000.
Настроить SSL: Выпустить и прикрутить бесплатный SSL-сертификат через Certbot (Let's Encrypt), чтобы дашборд работал по безопасному протоколу https://.
Как только домен будет привязан, дай знать, пожалуйста — мне нужно будет поменять пару строчек в конфигурации .env (обновить ссылки на API и CORS), чтобы приложение начало корректно с ним работать.

Спасибо!

Что вам нужно будет сделать после его работы:
Как только сисадмин скажет «готово», вам нужно будет зайти на сервер и обновить 2 вещи:

В файле .env фронтенда поменять NEXT_PUBLIC_API_URL с IP-адреса на новый домен.
Убедиться, что в main.py бэкенда в настройках CORS стоит allow_origins=["*"] (мы это уже ставили, так что должно работать из коробки).